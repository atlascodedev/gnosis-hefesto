{
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "name": "hefesto",
  "scripts": {
    "build": "lerna run build --stream",
    "dev": "lerna run dev --stream && firebase emulators:start",
    "deploy": "lerna run deploy --stream",
    "clean": "lerna clean -y && lerna bootstrap --hoist",
    "dev-hefesto": "lerna run dev-hefesto --stream",
    "build-hefesto": "lerna run build-hefesto --stream",
    "predev-hefesto": "echo hello world"
  }
}
