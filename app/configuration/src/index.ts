import { basePath, dashboardPath, loginRedirect, startingPath} from './routes.config'
import {adonisConfig, adonisImagePath} from './adonis.config'
import { brandingConfig} from "./branding.config"
import {collections} from "./collections.config"
import config from "./firebase.config"




export  {
    config,
    basePath,
    dashboardPath,
    loginRedirect,
    startingPath,
    adonisConfig,
    adonisImagePath,
    collections,
    brandingConfig
}